# 提交教學（Git 日常操作）

專案路徑：
```
C:\Users\Rui\Desktop\python工具\cyber-worklog-system
```

## 一、日常修改後提交（不發新版本）

打開 PowerShell，進到專案資料夾後依序輸入：

```powershell
cd C:\Users\Rui\Desktop\python工具\cyber-worklog-system

git status          # 看改動了哪些檔案
git add .           # 加入所有修改（或 git add 某個檔）
git commit -m "說明這次改什麼"
git push            # 上傳到 GitHub
```

> ⚠️ 上傳前用 `git status` 檢查，**不要**看到 `config/`、`*.db`、`venv/` 被追蹤——這些被 `.gitignore` 擋掉了，正常不會出現。

## 二、發新版本（v1.1.0、v2.0.0…）

三步，順序不能錯：

```powershell
git status          # 看改動了哪些檔案
git add VERSION src    # 版本號和程式碼
git commit -m "v1.1.0 新增功能"
git tag -a v1.1.0 -m "v1.1.0 更新說明"     # 標籤數字要跟 VERSION 檔一致
git push && git push origin v1.1.0          # 先推 main，再推標籤（或分開打）
```

建立 Release（使用者自動更新抓的就是這個）：

```powershell
gh release create v1.1.0 --title "v1.1.0 更新說明" --notes "詳細內容"
```

## 重點提醒

- `VERSION` 檔的數字、git 標籤、Release 名稱三者要**一致**，程式才會判斷有新版
- 已設定 remote，所以 `gh release create` 不需再加 `--repo ...`
- 想改錯的 commit：`git commit --amend -m "新說明"`（未 push 時才用）
- 想收回最後一次 commit：`git reset --soft HEAD~1`
- 遇到 [rejected] fetch first，先 git pull --rebase origin main 再 git push 即可。